*.sqlite filter=lfs diff=lfs merge=lfs -text
/Users/diogo/Dev/Elections/data/interim/news.sqlite filter=lfs diff=lfs merge=lfs -text

# Jupyter notebook (to fix Github issues with Jupyter notebook line count)

# For text count
*.ipynb  text

# To ignore it use below
*.ipynb linguist-documentation
